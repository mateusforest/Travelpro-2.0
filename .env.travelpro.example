# Extra variable for encrypting integration credentials at rest.
# Generate 32 random bytes, encode as 64 hex characters, keep private.
# Use the same key on every server sharing this database. Do not rotate casually.
TRAVELPRO_ENCRYPTION_KEY=
